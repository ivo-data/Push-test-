 cat = mostra o que tem; >> = adiciona no final do arquivo; git checkout = cria novas brances; git reset = ultimo estado; git brench = lista de branches; git merge = mesclagem de 2 branches; git branch -b = delte; git status -s= mostra os arquivos ocultos; git add . = adiciona na area stage; git diff = alterações ou diferencias no arquivo; git comit -m = messamgem descrição; gti comit = o que possue; git init = cria uma pasta GIT; mkdir = cria um diretorio; remove = apaga arquivos; rm -r = apaga arquivos especificos; git cat = caracteristicas do arquivo
